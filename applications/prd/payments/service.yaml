apiVersion: v1
kind: Service
metadata:
  name: payments
  namespace: payments-prd
  labels:
    app: payments
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: payments
