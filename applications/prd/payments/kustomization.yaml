apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: payments-prd

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      environment: production
      managed-by: argocd

images:
  - name: acrwedigitekprd.azurecr.io/payments
    newTag: latest
