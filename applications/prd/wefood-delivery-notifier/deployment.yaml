apiVersion: apps/v1
kind: Deployment
metadata:
  name: wefood-delivery-notifier
  namespace: wefood-delivery-notifier-prd
  labels:
    app: wefood-delivery-notifier
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: wefood-delivery-notifier
  template:
    metadata:
      labels:
        app: wefood-delivery-notifier
    spec:
      containers:
      - name: wefood-delivery-notifier
        image: acrwedigitekprd.azurecr.io/wefood-delivery-notifier:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 3000
          protocol: TCP
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
