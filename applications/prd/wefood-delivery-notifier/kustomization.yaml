apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wefood-delivery-notifier-prd

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: wefood-delivery-notifier
  environment: production
  managed-by: argocd

images:
  - name: acrwedigitekprd.azurecr.io/wefood-delivery-notifier
    newTag: latest
