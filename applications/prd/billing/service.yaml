apiVersion: v1
kind: Service
metadata:
  name: billing
  namespace: billing-prd
  labels:
    app: billing
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: billing
