apiVersion: apps/v1
kind: Deployment
metadata:
  name: wefood-realtime-server
  namespace: wefood-realtime-server-prd
  labels:
    app: wefood-realtime-server
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: wefood-realtime-server
  template:
    metadata:
      labels:
        app: wefood-realtime-server
    spec:
      volumes:
      - name: keyvault-env
        csi:
          driver: secrets-store.csi.k8s.io
          readOnly: true
          volumeAttributes:
            secretProviderClass: wefood-realtime-server-keyvault-env
      containers:
      - name: wefood-realtime-server
        image: acrwedigitekprd.azurecr.io/wefood-realtime-server:latest
        imagePullPolicy: Always
        envFrom:
        - secretRef:
            name: wefood-realtime-server-secrets-kv
        volumeMounts:
        - name: keyvault-env
          mountPath: /mnt/keyvault-env
          readOnly: true
        ports:
        - name: http
          containerPort: 3000
          protocol: TCP
        resources:
          requests:
            memory: "256Mi"
            cpu: "25m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 120
          periodSeconds: 30
          timeoutSeconds: 15
          failureThreshold: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
