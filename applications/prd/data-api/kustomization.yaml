apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: data-api-prd

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: data-api
  environment: production
  managed-by: argocd

images:
  - name: acrwedigitekprd.azurecr.io/data-api
    newTag: latest
