apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: notifications-service-prd

resources:
  - secretproviderclass-env.yaml
  - namespace.yaml
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      environment: production
      managed-by: argocd

images:
  - name: acrwedigitekprd.azurecr.io/notifications-service
    newName: ghcr.io/wedigitek/we-services-notifications
    newTag: rc
