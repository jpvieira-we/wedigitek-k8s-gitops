apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: notifications-service-prd
  labels:
    app: notifications-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: notifications-service
