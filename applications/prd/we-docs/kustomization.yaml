apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: we-docs-prd

resources:
  - namespace.yaml
  - secretproviderclass-env.yaml
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      environment: production
      managed-by: argocd

images:
  - name: ghcr.io/wedigitek/we-docs
    newTag: latest
