apiVersion: apps/v1
kind: Deployment
metadata:
  name: activity-logs-service
  namespace: activity-logs-service-prd
  labels:
    app: activity-logs-service
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: activity-logs-service
  template:
    metadata:
      labels:
        app: activity-logs-service
    spec:
      containers:
      - name: activity-logs-service
        image: acrwedigitekprd.azurecr.io/activity-logs-service:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 3000
          protocol: TCP
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
