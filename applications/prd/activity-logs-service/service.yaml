apiVersion: v1
kind: Service
metadata:
  name: activity-logs-service
  namespace: activity-logs-service-prd
  labels:
    app: activity-logs-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: activity-logs-service
