apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: quality-control-prd

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: quality-control
  environment: production
  managed-by: argocd

images:
  - name: acrwedigitekprd.azurecr.io/quality-control
    newTag: latest
