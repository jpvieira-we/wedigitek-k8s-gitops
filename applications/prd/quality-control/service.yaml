apiVersion: v1
kind: Service
metadata:
  name: quality-control
  namespace: quality-control-prd
  labels:
    app: quality-control
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: quality-control
