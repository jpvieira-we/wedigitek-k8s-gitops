apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: we-admin-prd

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: we-admin
  environment: production
  managed-by: argocd

images:
  - name: acrwedigitekprd.azurecr.io/we-admin
    newTag: latest
