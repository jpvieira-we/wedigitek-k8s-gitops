apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: reports-prd

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: reports
  environment: production
  managed-by: argocd

images:
  - name: acrwedigitekprd.azurecr.io/reports
    newTag: latest
