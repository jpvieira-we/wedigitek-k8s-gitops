apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: we-api-integrations-service-prd

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

labels:
  - pairs:
      environment: production
      managed-by: argocd

images:
  - name: acrwedigitekprd.azurecr.io/we-api-integrations-service
    newTag: latest
