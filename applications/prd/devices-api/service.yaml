apiVersion: v1
kind: Service
metadata:
  name: devices-api
  namespace: devices-api-prd
  labels:
    app: devices-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: devices-api
