apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: devices-api-prd

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app: devices-api
  environment: production
  managed-by: argocd

images:
  - name: acrwedigitekprd.azurecr.io/devices-api
    newTag: latest
