apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: we-api-prd

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - ingress.yaml

commonLabels:
  app: we-api
  environment: production
  managed-by: argocd

images:
  - name: acrwedigitekprd.azurecr.io/we-api
    newTag: latest  # Será atualizado via CI/CD com hash do commit
