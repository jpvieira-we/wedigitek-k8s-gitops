apiVersion: v1
kind: Service
metadata:
  name: access-control
  namespace: access-control-prd
  labels:
    app: access-control
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: access-control
